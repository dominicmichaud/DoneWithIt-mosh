import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        padding: 15,
    },
    detailsContainer: {
        justifyContent: 'center',
        marginLeft: 10,
    },
    detailsHeading: {
        marginTop: 6,
    },
    image: {
        width: 70,
        height: 70,
        borderRadius: 35,
    },
});