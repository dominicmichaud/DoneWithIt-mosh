import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    animation: {
        width: 140,
    },
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center'
    },
});