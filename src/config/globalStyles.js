import theme from '../../config/theme';
const { heading, darkGrey } = theme;

export default {
    color: theme,
    text: {
        fontSize: 18,
        fontFamily: heading,
        color: darkGrey,
    },
}